import express from 'express';
// import movieController from '../controller/movieController';

const router = express.Router();

// router.post('/comment' movieController.comment);

export default router;
